vimfilesdir = get_option('datadir') / 'vim' / 'vimfiles'

install_data('ftdetect/pkgaddconf.vim', install_dir: vimfilesdir / 'ftdetect')
install_data('syntax/pkgaddconf.vim',   install_dir: vimfilesdir / 'syntax')
